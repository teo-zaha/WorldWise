import { Routes } from '@angular/router';
import { GermanStatesComponent } from '../pages/german-states/german-states.component';

export const routes: Routes = [
    {path: "german-states", component: GermanStatesComponent}
];
